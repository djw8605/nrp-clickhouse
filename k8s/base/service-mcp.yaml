apiVersion: v1
kind: Service
metadata:
  name: nrp-accounting-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
